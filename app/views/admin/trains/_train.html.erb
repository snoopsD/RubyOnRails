<tr>
  <td><%= train.name %></td>
  <td><%= link_to t('common.show'), [:admin, train] %></td>
  <td><%= link_to t('common.edit'), edit_admin_train_path(train) %></td>
  <td><%= link_to t('common.delete'), [:admin, train], method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
